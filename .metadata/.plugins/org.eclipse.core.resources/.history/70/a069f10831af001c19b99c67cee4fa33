package builder;

import entity.Address;

import entity.Immobile;

public interface IBuilder  <T extends Immobile> {//ex immobile
	//public interface IBuilder  <T extends AbstractBuilder<T>> {//ex immobile
	
	IBuilder<T> surface(int surface);

	IBuilder<T> localNumber(int localNumber);

   IBuilder<T> address(Address address);

	IBuilder<T> type(String type);
	
	T build();
	
}
